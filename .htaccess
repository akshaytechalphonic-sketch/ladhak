# RewriteEngine On
# RewriteCond %{REQUEST_URI} !^/public/
# RewriteRule ^(.*)$ public/$1 [L]


# <IfModule mod_expires.c>
#     ExpiresActive Off
# </IfModule>
 
# <IfModule mod_headers.c>
#     Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
#     Header set Pragma "no-cache"
#     Header set Expires 0
# </IfModule>
# <FilesMatch "\.(html|php|css|js|jpg|png|gif)$">
#     Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
#     Header set Pragma "no-cache"
#     Header set Expires 0
# </FilesMatch>

# <IfModule mod_rewrite.c>
#     RewriteEngine On

#     # Prevent directory listing
#     Options -Indexes

#     # Block access to sensitive files
#     <FilesMatch "^\.env|composer\.(json|lock)|package\.(json|lock)|artisan|phpunit\.xml">
#         Order allow,deny
#         Deny from all
#     </FilesMatch>

#     # Block access to hidden directories (.git, .vscode, etc)
#     # Allows .well-known for SSL certificates
#     RewriteRule ^\.(?!well-known).* - [F,L]

#     # Route everything to public folder
#     RewriteRule ^(.*)$ public/$1 [L]
# </IfModule>

# <IfModule mod_expires.c>
#     ExpiresActive Off
# </IfModule>

# <IfModule mod_headers.c>
#     Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
#     Header set Pragma "no-cache"
#     Header set Expires 0

#     # Security Headers — prevent clickjacking, MIME sniffing, XSS, and data leakage
#     # Header always set X-Frame-Options "SAMEORIGIN"
#     Header always set X-Content-Type-Options "nosniff"
#     Header always set X-XSS-Protection "1; mode=block"
#     Header always set Referrer-Policy "strict-origin-when-cross-origin"
#     Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"

# </IfModule>

<IfModule mime_module>
  AddHandler application/x-httpd-ea-php82___lsphp .php .php8 .phtml
</IfModule>
# php -- END cPanel-generated handler, do not edit

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent directory listing
    Options -Indexes

    # Block access to sensitive files
    <FilesMatch "^\.env|composer\.(json|lock)|package\.(json|lock)|artisan|phpunit\.xml">
        Order allow,deny
        Deny from all
    </FilesMatch>

    # Block access to hidden directories (.git, .vscode, etc)
    RewriteRule ^\.(?!well-known).* - [F,L]

    # FIX: Avoid infinite routing issue
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]

</IfModule>

<IfModule mod_expires.c>
    ExpiresActive Off
</IfModule>

<IfModule mod_headers.c>
    Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
    Header set Pragma "no-cache"
    Header set Expires 0

    # ✅ IMPORTANT FIX: Allow iframe (Google Maps)
    Header always unset X-Frame-Options

    # ✅ Allow Google Maps and Razorpay iframe (CSP)
    Header always set Content-Security-Policy "frame-src https://www.google.com https://maps.google.com https://www.google.co.in https://api.razorpay.com https://checkout.razorpay.com https://*.razorpay.com;"

    # Other security headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# php -- BEGIN cPanel-generated handler, do not edit
# Set the “ea-php82” package as the default “PHP” programming language.
<IfModule mime_module>
  AddHandler application/x-httpd-ea-php82___lsphp .php .php8 .phtml
</IfModule>
# php -- END cPanel-generated handler, do not edit
